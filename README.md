# sqscpp

## Local Development

Start the development docker container:

```sh
docker compose run --rm app
```

or use the shortcut:

```sh
make dev
```

Then, from the container shell:

```sh
make build
```
